package com.example.componentframework

interface SAFComponent{
    fun parsePipeline(pipeline: String): String{
        /**
         * for(pipeline)
         * extractValueFromPipeline()
         */
        return "Address"
    }
}